import Message from "../message/Message.tsx";

type Props = {};

const Content = (props: Props) => {
    return (
        <section className={''}>
            <Message/>
        </section>
    )
};

export default Content;